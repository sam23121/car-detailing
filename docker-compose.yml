# Backend only. Run from repo root: docker compose up -d
# Uses backend/.env for DATABASE_URL, SMTP, Twilio, etc.

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
