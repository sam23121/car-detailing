# Backend API base URL (no trailing slash).
# All frontend API requests use this. Omit for local dev (defaults to http://localhost:8000).
# VITE_API_URL=http://localhost:8000
# VITE_API_URL=https://api.yoursite.com
